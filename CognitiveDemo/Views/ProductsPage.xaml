<?xml version="1.0" encoding="UTF-8"?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="CognitiveDemo.ProductsPage"
             Title="{Binding Title}"
		     xmlns:local="clr-namespace:CognitiveDemo;assembly=CognitiveDemo">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Identify" Command="{Binding FindSimilarFaceCommand}"/>
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<StackLayout>
			<ListView ItemsSource="{Binding Products}" HasUnevenRows="true" SeparatorVisibility="None">
				<ListView.ItemTemplate>
					<DataTemplate>
						<local:CardViewCell Text="{Binding Name}" Detail="{Binding Title}" ImageSource="{Binding Image}"/>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentPage.Content>
</local:BasePage>
