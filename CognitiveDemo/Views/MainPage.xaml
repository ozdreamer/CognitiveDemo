<?xml version="1.0" encoding="utf-8"?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="CognitiveDemo.MainPage"
                xmlns:local="clr-namespace:CognitiveDemo;assembly=CognitiveDemo">
    <ContentPage.Content>  
        <StackLayout Orientation="Horizontal" Padding="20,0,20,0" VerticalOptions="Center">
            <Button Command="{Binding SignInCommand}" Text="Sign In" TextColor="White"  
                FontAttributes="Bold" FontSize="Medium" HorizontalOptions="FillAndExpand"  
                BackgroundColor="Green" />  
            <Button Command="{Binding SignUpCommand}" Text="Sign Up" TextColor="White"  
                FontAttributes="Bold" FontSize="Medium" HorizontalOptions="FillAndExpand"  
                BackgroundColor="Blue" />  
        </StackLayout>
    </ContentPage.Content>  
</local:BasePage>